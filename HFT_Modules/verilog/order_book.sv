`include "sys_defs.svh"
module order_book (
    input   logic               clk,
    input   logic               rst_n,
    input   order_lookup_out_t  lookup_info

);

always_comb begin
    if (lookup_info.valid) begin
        if (lookup_info.)
    end
end


endmodule